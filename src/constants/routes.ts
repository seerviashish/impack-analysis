const STUDENT: string = "/student/:id";
const REJECTED: string = "/rejected";
const HOME: string = "/";
const ERROR: string = "/error";
const SHORTLISTED: string = "/shortlisted";

export { HOME, STUDENT, REJECTED, ERROR, SHORTLISTED };
